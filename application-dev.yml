spring:
  application:
    name: CONFIG-DEV

##eureka setup
eureka:
 instance:
   prefer-ip-address: true
#    hostname: localhost
 client:
   fetch-registry: true
   register-with-eureka: true
   service-url:
     defaultZone: http://localhost:8761/eureka
